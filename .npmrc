verify-deps-before-run=false
# Hoist node_modules flat (npm-like) so Next's server can resolve vendor chunks
# (e.g. @formatjs used by next-intl) under pnpm. Avoids runtime
# "Cannot find module './vendor-chunks/@formatjs+...'" 500s.
node-linker=hoisted
